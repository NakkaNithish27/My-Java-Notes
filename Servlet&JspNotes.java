/*Servelet is a java file that do all the processing and generates a static html page as a response
  Apache Tomcat(web container) server processes these servelet file*/

//Download eclipse EE>download and configure tomcat>create a new dynamic web application

//In web.xml (deployment descriptor we do all the servlet mapping) 
